<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- The diagnostics dir under the private internal filesDir, shared via FileProvider. -->
    <files-path name="diagnostics" path="diagnostics/" />
</paths>
